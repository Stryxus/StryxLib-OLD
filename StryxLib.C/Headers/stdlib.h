// This file contains all C Standard Headers available
#ifndef __STDLIB__
#define __STDLIB__
#include <assert.h>      // (C90) Conditionally compiled macro that compares its argument to zero
#include <complex.h>     // (C99) Complex number arithmetic
#include <ctype.h>       // (C90) Functions to determine the type contained in character data
#include <errno.h>       // (C90) Macros reporting error conditions
#include <fenv.h>        // (C99) Floating - point environment
#include <float.h>       // (C90) Limits of floating - point types
#include <inttypes.h>    // (C99) Format conversion of integer types
#include <iso646.h>      // (C95) Alternative operator spellings
#include <limits.h>      // (C90) Ranges of integer types
#include <locale.h>      // (C90) Localization utilities
#include <math.h>        // (C90) Common mathematics functions
#include <setjmp.h>      // (C90) Nonlocal jumps
#include <signal.h>      // (C90) Signal handling
#include <stdalign.h>    // (C11) alignas and alignof convenience macros
#include <stdarg.h>      // (C90) Variable arguments
#include <stdatomic.h>   // (C11) Atomic operations
#include <stdbool.h>     // (C99) Macros for boolean type
#include <stddef.h>      // (C90) Common macro definitions
#include <stdint.h>      // (C99) Fixed - width integer types
#include <stdio.h>       // (C90) Input / output
#include <stdlib.h>      // (C90) General utilities : memory management, program utilities, string conversions, random numbers, algorithms
#include <stdnoreturn.h> // (C11) noreturn convenience macro
#include <string.h>      // (C90) String handling
#include <tgmath.h>      // (C99) Type - generic math(macros wrapping math.h and complex.h)
#include <threads.h>     // (C11) Thread library
#include <time.h>        // (C90) Time / date utilities
#include <uchar.h>       // (C11) UTF - 16 and UTF - 32 character utilities
#include <wchar.h>       // (C95) Extended multibyte and wide character utilities
#include <wctype.h>      // (C95) Functions to determine the type contained in wide character data
#endif